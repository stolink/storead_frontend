## 📋 변경 사항

지불 시스템 통합 및 프리미엄 UI/UX 개선 작업을 완료했습니다.

### 1. 토스 페이먼츠 통합 (Toss Payments Integration)

- **SDK 전환**: `payment-widget-sdk` → `tosspayments-sdk`(일반 결제)로 전환하여 키 호환성 해결
- **결제 프로세스**: 팝업 방식 카드 결제 구현 및 성공/실패 페이지 연동
- **안정성 강화 (AI Review 반영)**: React StrictMode에서 결제 승인 API가 중복 호출되는 문제를 `useRef`로 해결

### 2. 프리미엄 UI/UX 고도화

- **Pretendard 폰트 도입**: 서비스 전체 타이포그래피 현대화
- **결제 위젯 리디자인**: 글래스모피즘 기반 "프리미엄 영수증" 디자인 적용
- **사용자 정보 연동**: 결제 시 실제 유저 이메일/닉네임이 Toss 시스템에 기록되도록 수정
- **상태 관리 최적화**: 렌더링 단계에서 상태를 업데이트하던 안티패턴(`ChapterRatingCard`)을 `useEffect`로 수정

### 3. 개발 환경 및 안정성

- **Vite 설정**: OAuth 리디렉트 불일치 방지를 위해 `strictPort: 5174` 강제
- **빌드 테스트**: `npm run build`를 통한 타입 안정성 및 린트 검증 완료
- **트러블슈팅 문서화**: `.troubles/` 디렉토리에 주요 이슈 해결 과정 기록

## 📁 변경된 파일

- `src/components/payment/PaymentWidget.tsx`: 유저 데이터 연동 및 UI 개선
- `src/pages/payment/PaymentSuccessPage.tsx`: 중복 호출 방지 로직 추가
- `src/components/rating/ChapterRatingCard.tsx`: 리액트 권장 생명주기 패턴 적용
- `docs/TROUBLESHOOTING.md` & `.troubles/*`: 최신 장애 대응 로그 추가
- `index.html`, `src/index.css`, `tailwind.config.ts`: 글로벌 스타일 및 설정

## ✅ 체크리스트

- [x] 빌드 성공 확인 (`npm run build`)
- [x] 로컬 테스트 완료 (결제 승인 흐름 및 UI 확인)
- [x] OAuth 리디렉트 URI 설정 확인

## 🔀 Merge 가이드

- Target: `dev`
- Squash and Merge 권장
